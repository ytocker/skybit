# Sasa-Yuki-Onna — AD critique round 1

VERDICT: ITERATE

Strong characterful first pass with good botanical DNA, but trips the set's hardest gate (flipped
dominance vs Yukitake) and partially loses the KIND at true 32px. Both fixable in one round.

**Strongest:** WIDE-LOW mound silhouette KIND is correct and unique; ~7 fanning lance-clumps holds;
clean hard-stepped leaf shading; periwinkle-faced spirit charming; pillar logic legible. Cool winter
temperature cleanly separated from #7's warm ambush read (#3-vs-#7 gap holds).

**Weakest (the gate):** snow has rebuilt a mini-Yukitake — central band measured 49% snow vs 18% in
outer blades. The white core is the focal mass; sage is relegated to the leaf-blades. Reads "white
spirit with green leaves" — exactly the Yukitake failure mode the pin exists to prevent.

## Round-2 punch list (in order)
1. **Flip the dominance (gate):** halve the central white core; expose muted-sage `(120,156,118)` as
   the visible mound BODY. Target green ≥55% of creature mass, snow ≤30%.
2. Break snow into ≥5 DISCRETE hard powder-caps on the mound + leaf-tips — no connected white belly.
3. Rebuild the face snow-RIMMED not snow-filled: shrink it, embed in the sage drift, make periwinkle
   `(176,200,236)` (not white) its dominant value — the one place the eye lands.
4. Day-chip rescue: drop to ~5 BIGGER, more angular blade-clumps so the spiky-mound KIND survives the
   32px downsample (day chip currently reads as a grey-white smudge — judge it hardest next round).
5. Add the dry pale-margin `(214,220,194)` hard band on leaf edges (real-sasa tell + a value step).
6. Re-verify the blacked-out silhouette stays WIDE-LOW + spiky after the sage body grows.

Night chip + blacked-out silhouette already read well — protect them.
