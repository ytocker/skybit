# Asthi-Dakini — CHOSEN revision

**Locked: `SWITCHED + BIG`.**

- Render: [`render_switchbig.py`](render_switchbig.py)
- Hero: [`round_1_switchbig_hero.png`](round_1_switchbig_hero.png)
- Sheet: [`round_1_switchbig.png`](round_1_switchbig.png)
- Picked from the 4-up [`compare.png`](compare.png) (base / ring-eye / switched / switched+big).

## What it is (deltas vs the canonical `mukha_citipati_court/asthi_dakini` round 10)

1. **Third-eye = the LARGER faceted cyan cut-gem** (`r = 10·s`), the single brightest
   point (white-hot core) — the dominant focal at the head.
2. **Necklace heart = the concentric-RING "iris"** (`ring_eye`, `r = hr·0.30`): the
   earlier (darker-skin, round-3) third-eye SHAPE — gold iris ring + cyan disc + pale
   pupil — reproduced CLEAN, with **no additive blue glow aura**.
3. Warm-ivory skin (base); plain (un-restyled) skulls.

Value ladder held: faceted third-eye gem (white core) > necklace ring > skull cyan.

## Status

Asthi-Dakini is a `skybit_devil` design exploration; it is **not yet wired into the
live game** (`game/` has no asthi reference). This is the locked art direction for
whenever it's integrated. Open follow-ups (not done — awaiting the go-ahead):
- fold this revision into the canonical `asthi_dakini` lineage as its newest round, and/or
- wire it into the game when Asthi is scheduled in.
