VERDICT: ITERATE

Asks 2 (palm-skulls) and 3 (hands) landed well; ask 1 (faceted brow gem — the gate) FAILED: the
gem is still a glossy orb. Gem-only re-cut round.

## Ask 1 — Faceted GEM: FAILED (the asthi orb trap, repeated)
The brow focal is still a smooth glossy violet ORB — one soft round white highlight floating
top-left inside a gold OVAL ring, gradient falloff to darker violet below = a glass-marble/camera-
lens read. Geometry unchanged vs round_2 (round girdle, round highlight, no facets, no flat table,
no stepped values, no sharp corner glints). It IS the single brightest pixel (good) but the FORM
is wrong. Fix is structural, matching asthi round 5:
- **Octagonal/hexagonal girdle** — kill the oval gold ring; the stone silhouette has flat angled sides.
- **Flat polygonal TABLE** on top (single lighter-violet plane, hard straight edges, not a soft blob).
- **4-6 stepped crown facets** around the table — each a DIFFERENT discrete flat violet value, NO
  gradient inside a facet (the value steps sell "cut").
- **2-3 dark pavilion facets** converging to a bottom culet (lower half angular, not rounded).
- **1-2px hard white corner GLINTS** pinned to facet edges/corners (one becomes the single
  brightest pixel) — not a soft central highlight.
- Keep its own violet hue + brightest element; don't let glints exceed the gem's brightness budget
  (preserve brow gem → palm-skull gems mid → crown dimmest).
Pull asthi round 5's `cyan_gem()` facet geometry + value stepping directly, recolored violet.

## Ask 2 — Palm-skulls: KEEP (the round's win)
Noticeably smaller relative to the hand; each a crafted tiny skull (suture/temple/brow/jaw/teeth)
with genuine variety; ≥2-3 carry a dim violet brow-gem element, correctly dimmer than the brow gem.

## Ask 3 — Hands: KEEP (minor)
Finer hands — wrist-cuff + half-cup + individually knuckled fingers, hand the larger leading shape
cradling the smaller skull. Minor: lower-left hand's fingertip dots merge slightly into the skull;
nudge them ~1px apart so the cup edge stays distinct (non-blocking).

## KEEP (not regressed)
Cinnabar brocade vajra-silk (chest X + bell skirt + saffron florets); open ember flame-ring;
cinnabar+saffron palette (violet gem = lone cool accent, no fight); CITIPATI body; six-arm fan;
fused 5-skull arc + tiara-band crown; mirrored pillar; 32px wide-skirt silk carrier; chibi
scary-cute. Value ladder correct — preserve exactly when re-cutting the gem.

## Punch list (gem-only re-cut)
1. Re-cut the brow gem orb → faceted cut jewel (octagonal girdle + flat table + 4-6 stepped flat
   crown facets + 2-3 pavilion facets to a culet + hard corner glints). Match asthi round 5. THE GATE.
2. Keep it violet + brightest; glints must not blow the ladder.
3. Minor: nudge the lower-left fingertip dots ~1px apart.
4. Do NOT touch the palm-skulls / silk / crown / flame / palette — gem-only round.
