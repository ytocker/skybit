# Kadomatsu-Torii — AD critique round 3 (FINAL)

VERDICT: SHIP-READY

The two round-2 gate items (where this concept was blocking) are demonstrably fixed.

1. **Four discrete cream nodes, top AND bottom CLEARED:** on day + night 32px chips I count FOUR distinct
   cream blobs with dark gaps — two up top (lintel-ends outboard, leg-tops inboard/down so they no longer
   kiss the lintel) + two capping the leg feet. The round-2 fused cream top-BAR + bare bottom corners is
   gone → reads "open rectangle pinned at four corners."
2. **Open-gate blackout CLEARED (strongest chip in the set):** four distinct corner lobes, thinned lintel
   with a negative-space sliver, doorway holds wide; no top-heavy fused blob.
3. **Bound-culm legs (not timber) CLEARED:** hero legs are 3-stripe bundles; the deepened inter-culm seam
   survives 32px as visible green segmentation (reads bamboo, not post).
4. **Cream out-ranks vermilion** for the RIGHT reason now (four distributed corner anchors, not a top
   slab; vermilion cord darkened/narrowed as secondary). Under-beam nubs gone; top de-weighted by foot discs.

Residual polish (NON-blocking): (1) on bright biomes the two top discs + lintel can momentarily smear
before the eye separates them — a 1px ink keyline on the top-disc undersides would lock the gap (night +
blackout separate cleanly); (2) faint plum fringe at the foot-disc bases — keep subordinate in any recolor.
Pillar variant reads beautifully, no notes.

Final = round 3. SHIP-READY.
