# Kadomatsu-Torii — AD critique round 2

VERDICT: ITERATE (final GD round 3 — a disc-deployment + downscale pass, not a rebuild)

Real material progress: the legs are now genuine striped culm bundles at hero and the discs read as
steep sogi cuts (not glossy buttons). But the load-bearing thesis failed at 32px: the four discs merge
into one cream BAR clamped across the top, with ZERO cream at the bottom corners — "open rectangle
pinned by FOUR cream nodes" is not there. Blackout shows the top corners fused into one heavy black blob.

Gate check: bound-culm legs CLEARED at hero, marginal at 32px (stripe/seam vanishes → plain green posts).
Four-corner discs FAILED at 32px + blackout (top-only, fused). Cream out-ranks vermilion but for the
WRONG reason (massed into a top slab). Doorway negative space PASSES at day/night/blackout (keep it).

KEEP: hero bound-culm leg material; the sogi-slant cut treatment; narrowed vermilion cord + base
plum/pine; the open gold-lit doorway.

## Round-3 punch list (final — items 1+2 are the gate)
1. **SEPARATE the four discs into four discrete nodes (not a top bar):** pull the lintel-end discs
   OUTBOARD (overhang past the legs) and the leg-top discs slightly inboard/down so there's clear
   green/sky gap between each pair. At 32px you must count FOUR cream blobs with dark gaps. Verify on the
   32px chip before anything else — this is ship-or-not.
2. **Get cream onto the BOTTOM corners:** cap each leg FOOT with a small diagonal-cut cream disc (a cut
   culm-base) above/among the plum, so the read is top-discs + foot-discs = a true pinned rectangle. Plum
   stays as base life but must NOT out-bright the foot cream.
3. **Break up the top black mass for blackout:** after separating discs, re-confirm blackout reads as an
   open frame with distinct corner lobes; if lintel + top discs still merge, thin the lintel + add a
   sliver of negative space between disc-tops and the lintel line.
4. **Lift the leg-stripe read at 32px ~one step:** deepen the inter-culm seam (`CULM_DD`) + widen it 1px
   so the bundle seam survives downscale (a leg is never a flat post).
5. **Remove the remaining 2–3 under-beam cream nubs** (still reading as teeth/noise — round-1 note only
   half-actioned).
6. De-weight the top slightly (the foot discs from #2 do most of this) so the gate isn't top-heavy at hero.

Bones, material, cut-treatment all right — purely a disc-deployment-and-downscale round.
