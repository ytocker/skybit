# Kadomatsu-Torii — AD critique round 1

VERDICT: ITERATE

Strong first round — the negative-space doorway is genuinely banked, the blackout silhouette is
unmistakably a torii (best-in-brood distinctness), base-balance is good. Fails the HARD GATE in two
fixable ways: the four corner cut-discs aren't deployed AS the corners, and the legs read as smooth
green posts (timber-skinned-green) rather than bound culm.

KEEP: wide gold-lit doorway + thin-member openness (banks at 32px day+night); the blackout torii
silhouette; the vermilion shimenawa as the single high red sash with plum/pine base-anchoring (not
top-heavy); day/night value hold + warm rim-keyline.

## Round-2 punch list (priority)
1. **Bound-bamboo, not timber (GATE #1):** rebuild each leg as 2–3 VERTICAL value-stepped culm stripes
   (a bundle seen side-on) with clear node collars + branch-stub nubs + a straw lashing band at each
   lintel-to-leg joint. Current legs are smooth posts with faint node lines → reads as painted dowel.
2. **Move two discs to the leg TOPS:** each upright terminates in its own diagonal-cut cream disc BEFORE
   meeting the lintel; the two lintel ends get the other two → true four-corner deployment (currently
   all four are bunched at the lintel line; bottom of gate has zero cream).
3. **Enlarge + spread the corner discs, push `CUT_HI` to the brightest value** so at 32px the read is
   four discrete cream dots at four corners (not one bright horizontal blob smearing into the sash).
   Verify on both chips.
4. **Re-render discs as steep sogi cuts** (offset bright cavity, hard steps) — not pillowed glossy domes
   (currently read like buttons/grommets). Match the parent `diagonal_cut` cavity treatment.
5. **Narrow the vermilion to a true cord/sash band** so the cream out-ranks it as focal (area ratio is
   currently inverted — red slab dominates).
6. Resolve or remove the small white nubs hanging under the lintel/tie-beam (read as teeth/noise).
7. Re-confirm blackout still reads as an open four-cornered gate after the disc move; tidy the pillar
   lower-mirror foot so its gap-cap cream disc stays the brightest point.

Bones are right + doorway banked — a deployment-and-material round, not a re-roll.
