# Shishi-Kadomatsu — AD critique round 5 (FINAL — shipped with flag)

VERDICT: ITERATE → SHIPPED AS-IS WITH FLAG (pre-authorized in round 4; budget spent)

The one scoped gate — crown TOP-arc toothing in pure black — missed a third time. Pixel-level top contour
is still a single smooth monotone arc; the round-5 notches landed on the SHOULDER/side step and the
inter-disc gaps (which sit below the outer disc arc and get domed over), not the outer rim of the top
discs. To break the top arc you must cut 2px sky into the OUTER RIM of each upper cut-disc (rows ~548-552),
not the gaps between them — out of scope for the single-fix budget and not worth another re-roll.

**No regression (all KEEP):** four evenly-spaced black leg-posts under a low-wide komainu body; crown still
wider-than-tall (outer tips not shaved); no bright center face-window; 32px day+night color reads both
ship-quality (bound-culm quadruped + cream cut-disc crown as the brightest cluster, legible on both skies).

**Why shipped:** the pure-black top-contour scallop is a silhouette-QA nicety, NOT an in-game artifact —
the boss renders in COLOR, where the value/hue separation of the cream crown carries the silhouette and the
day/night reads are already ship-quality. The dome is invisible as a defect at gameplay scale + in motion.

If ever revisited: cut 2px sky into the outer RIM of the 3 upper cut-discs (rows ~548-552) + a 1px lift at
the dead-center cap apex so toothing reads as discrete tips.

Final = round 5 (color ship-quality; pure-black crown contour is a documented non-blocking nuance).
