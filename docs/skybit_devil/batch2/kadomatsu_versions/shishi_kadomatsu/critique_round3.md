# Shishi-Kadomatsu — AD critique round 3

VERDICT: ITERATE (shippable in color; one residual blackout item — orchestrator granted a scoped round 4)

Round 3 substantially recovered the blackout from round 2's domed-bell-with-window. Judging the pure-black
chip: FOUR posts PASS (the three negative-space slots split the skirt — the big win over r2); LOW WIDE body
PASS (wider-than-tall komainu raft); no bright center window PASS. SCALLOPED BRIGHT crown PARTIAL/FAIL —
the 1px ink gaps between outer discs compress away in pure black, so the crown still reads as a smooth dome
cap, not a toothed ring (the signature is carried by color/value, not silhouette). 3 of 4 blackout
conditions pass.

32px day+night creature read PASS: four green node-ringed leg-posts legible in both biomes; cream cut-disc
crown is the brightest cluster; not a winged medallion, not a green lion; night separates cleanly.

## Round-4 (scoped) punch list
1. **Crown scallop must be a SILHOUETTE feature, not a 1px rim trick:** cut 2px SKY gaps between the 3-4
   topmost outer discs so the BLACK contour itself goes bumpy (negative space, like the legs — the proven
   survivor). This is the one unmet gate.
2. Widen the center skirt slot ~1px so the four posts read evenly spaced (not 2+2 fused).
3. Kill two stray bright mid-body pixels (face + disc highlight) that float free at 1x.

Color read is already ship-quality; this pass is purely to land the blackout crown contour.
