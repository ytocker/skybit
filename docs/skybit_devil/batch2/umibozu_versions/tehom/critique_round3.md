VERDICT: ITERATE

# Tehom-no-Tamago — Round 3 critique (Umibozu-versions #5, abyssal leviathan-egg)

Final budgeted round. The literal R2 blocker is resolved, but the fix introduced a
near-identical value/hue defect one zone south. This SHIPS WITH A FLAG: the concept is
on-brief and the iris is fixed, but the downward crack-leak regressed from sour-green to
near-white and now carries a white belly-flash that re-creates the exact problem R2 was
killing — just relocated from the socket to the membrane. One more GD pass would close it;
absent budget, this is the open gap.

## R2 blocker verification (the one remaining item)

1. **White iris halo — FIXED, clean.** The eye is now a contained green pip: tight bone
   rim, sharp dark vertical slit, cooled/dimmed glow, no soft bloom spilling onto the whorl
   or shell. In the color 4x blow-up and the true-32px chips the iris reads as a small
   controlled sour-green accent, exactly as directed. The lid no longer reads as a hot disc.
   This was the blocker and it is genuinely gone.

## NEW regression introduced by the fix (the open gap)

2. **The downward crack-leak is now near-WHITE, not sour-green — REGRESSED.** The R2 note
   said the green crack should out-read the iris and be one of the two bright masses. Instead
   the membrane/crack-leak filling the lower-center of the egg is rendered near pure white
   with only thin green fringing. Evidence:
   - **Grayscale value check:** the bone whorl is correctly the brightest ring, but a
     near-white blob sits at the bottom-center, equal-to-or-brighter than the whorl and
     pulling the eye DOWN off the spiral.
   - **True 32px NIGHT chip:** the egg shows a hot white belly — on the dark night sky it is
     the brightest, hottest mass on the sprite. The spec pins the bright read as bone-whorl +
     **sour-green** crack `(150,206,140)`; white is not in Tehom's lane and reads as a generic
     glow rather than embryo-green light.
   This is the same class of defect as the R2 halo (an uncontrolled near-white mass winning the
   value read), moved from the iris socket to the crack-leak. The intent — dark shell, bone
   whorl + green crack carry the read — is locally broken in the lower egg.

## What's locked and must NOT be touched in any further pass

- Iris pip: contained, dim, sharp slit — locked. Do not re-brighten it.
- Continuous spiral whorl: single logarithmic coil, brightest ring — locked.
- Cap-shard + jagged split, dark shell perimeter value, green HUE separation vs Hamaguri,
  birth-cord pillar (twin braided strands + barnacle knots, clean mirror, shard cap leaking
  one line) — all locked and on-brief.

## The single fix to close the flag (if a pass is granted)

1. **Recolor the crack-leak/membrane from near-white to sour-green `(150,206,140)`** with its
   `(26,40,54)` dark-core behind it, so it reads as embryo-green light leaking from the crack —
   not a white belly. Keep one tiny bone-cream catch-pip if you want a glint, but the MASS must
   be green, not white.
2. **Shrink the leak footprint** to a tapering wedge below the eye (as the cap-shard leak does),
   so the dark shell re-owns the lower egg and the leak is an accent, not a fill.
3. **Re-check both value passes:** after the fix the bone whorl should be the brightest mass,
   the green crack the second, the iris a small accent — and NO near-white object anywhere on
   the egg in the grayscale check or the 32px night chip.

## Read at true 32px

KIND is unmistakable — cosmic spiral egg with a curious slit-eye, clearly distinct from
Hamaguri's horizontal fan-rib grin and the source Umibozu's smooth jelly-dome. House style
intact (flat triad, ink keyline, chibi scary-cute). The only thing keeping this from a clean
ship is the white belly: fix the leak hue and it's ship-ready.
