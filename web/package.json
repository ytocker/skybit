{
  "name": "skybit",
  "version": "3.0.0",
  "description": "Skybit — Pocket Sky Flyer (TypeScript + Canvas2D)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
